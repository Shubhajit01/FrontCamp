import { TrailingRemovePipe } from './trailing-remove.pipe';

describe('TrailingRemovePipe', () => {
  it('create an instance', () => {
    const pipe = new TrailingRemovePipe();
    expect(pipe).toBeTruthy();
  });
});
